# =========================================
# Agricultural RAG Chatbot — Requirements
# Python 3.11 | Tested on Windows + CUDA 12.1
# =========================================

# Core RAG
sentence-transformers==5.3.0
qdrant-client==1.17.1
ollama==0.6.1
langchain-text-splitters==1.1.1

# ML / GPU
# For CPU-only (Docker without GPU): pip install torch==2.5.1
# For GPU (CUDA 12.1): pip install torch==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1

# Data
numpy==2.2.6

# Web scraping / PDF pipeline
requests==2.32.5
beautifulsoup4==4.14.3
tqdm==4.67.3
docling==2.81.0

# UI
gradio==6.9.0
